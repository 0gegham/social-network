server:
  servlet:
    context-path: /api/v1/friends
  port: 8082

spring:
  application:
    name: friends-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://postgres:5432/snetwork
    username: username
    password: password
